import {
  PUBLIC_PHONE,
  PUBLIC_PHONE_DISPLAY,
  PUBLIC_TEXT,
  PUBLIC_TEXT_DISPLAY,
  PUBLIC_EMAIL,
  PUBLIC_CONSULTATION_URL,
} from '$env/static/public';

export const config = {
  phone:           PUBLIC_PHONE,
  phoneDisplay:    PUBLIC_PHONE_DISPLAY,
  text:            PUBLIC_TEXT,
  textDisplay:     PUBLIC_TEXT_DISPLAY,
  email:           PUBLIC_EMAIL,
  consultationUrl: PUBLIC_CONSULTATION_URL,
};
